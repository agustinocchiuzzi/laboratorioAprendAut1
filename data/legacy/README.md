# Datos heredados

Este directorio puede contener transformaciones manuales anteriores. No forma parte
del pipeline ni se versionan sus CSV. La unica fuente autoritativa es
`data/raw/futbol_uruguayo.zip`; todo archivo de `data/processed/` debe poder
regenerarse con `make clean-data`.

